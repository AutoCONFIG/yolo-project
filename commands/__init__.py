"""YOLO command modules.

Available modes:
    - train:    Model training
    - val:      Model validation
    - predict:  Model inference (images, video, ONNX, batch)
    - track:    Object tracking (video, with ByteTrack/BoT-SORT)
    - export:   Model export (ONNX, TensorRT, etc.)
"""
