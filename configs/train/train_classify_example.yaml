# YOLO Classification Training Configuration
# ===========================================
# Image classification training example.
# Dataset must follow ImageFolder structure: train/ and val/ with subdirs per class.

mode: train

model:
  name: yolo26n-cls.yaml
  pretrained: True
  task: classify

data:
  config: configs/datasets/classify_example.yaml
  split: val

train:
  epochs: 100
  time:
  batch: 64
  imgsz: 224
  device:
  workers: 8
  resume: false
  patience: 100
  cache: false
  save: true
  amp: true
  rect: false
  single_cls: false
  fraction: 1.0
  freeze:
  multi_scale: 0.0
  compile: false
  end2end:
  nbs: 64
  profile: false

  optimizer: auto
  lr0: 0.01
  lrf: 0.01
  momentum: 0.937
  weight_decay: 0.0005
  cos_lr: false

  warmup_epochs: 3.0
  warmup_momentum: 0.8
  warmup_bias_lr: 0.1

  close_mosaic: 10
  seed: 0
  deterministic: true

  dropout: 0.0
  overlap_mask: true
  mask_ratio: 4

augmentation:
  hsv_h: 0.015
  hsv_s: 0.7
  hsv_v: 0.4
  degrees: 0.0
  translate: 0.1
  scale: 0.5
  shear: 0.0
  perspective: 0.0
  flipud: 0.0
  fliplr: 0.5
  bgr: 0.0
  mosaic: 0.0
  mixup: 0.0
  cutmix: 0.0
  copy_paste: 0.0
  copy_paste_mode: flip
  auto_augment: randaugment
  erasing: 0.4

validation:
  val: true
  conf:
  iou: 0.7
  max_det: 300
  half: false
  plots: true
  save_json: false
  dnn: false
  agnostic_nms: false
  augment: false

cfg:
tracker: botsort.yaml

output:
  project:
  name: classify_example
  save_period: -1
  exist_ok: false
  verbose: true
